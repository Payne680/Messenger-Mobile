import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {},

    UserItemContainer: {
        display: 'flex',
        flexDirection: 'row',
        padding: 15,
    },

    userIcon: {
        width: 60,
        height: 60,
    },

    UserDetailsSection: {
        alignItems: 'center',
    }
});